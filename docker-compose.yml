version: '3.8'
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped
    tty: true
 
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5100:80"

volumes:
  db_data:
